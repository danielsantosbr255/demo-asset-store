services:
  backend-prod:
    build:
      context: ./backend
      target: prod
    image: demo-asset-store:prod
    env_file:
      - ./backend/.env
    ports:
      - 8080:8080
    networks:
      - network

networks:
  network:
